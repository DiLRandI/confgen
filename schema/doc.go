// Package schema compiles YAML configuration schemas for code generation.
// Compile parses and validates a schema in one call. Parse and Validate are
// available separately for tooling. Applications load generated Go instead.
package schema
